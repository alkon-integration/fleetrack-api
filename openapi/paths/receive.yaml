post:
  tags:
    - Message
  summary: Recibir mensaje
  description: |
    Recibir datos de guia, camion, fecha y direccion de la obra
  operationId: operationId
  security:
    - api_key: []
    - basic_auth: []
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/Receive.yaml
    description: requestBody description
    required: true
  responses:
    '200':
      description: OK
    '400':
      description: Error
